<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>EL DUCE</title>
  <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;700&family=Roboto&display=swap" rel="stylesheet">
  <style>
    /* --- Reset & Fonts --- */
    * { margin:0; padding:0; box-sizing:border-box; font-family: 'Roboto', sans-serif; }
    body { background-color: #0b0b0b; color: #f5f5f5; }

    /* --- Top Nav --- */
    .topnav {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 20px 40px;
      background-color: #1a1a1a;
      position: sticky;
      top: 0;
      z-index: 1000;
    }

    /* Hamburger menu */
    .hamburger {
      cursor: pointer;
      display: flex;
      flex-direction: column;
      gap: 5px;
    }
    .hamburger div {
      width: 25px;
      height: 3px;
      background-color: #d4af37; /* Gold */
      transition: 0.3s;
    }

    /* Dropdown menu */
    .dropdown {
      position: absolute;
      top: 60px;
      left: 40px;
      background-color: #1a1a1a;
      border: 1px solid #d4af37;
      display: none;
      flex-direction: column;
      min-width: 180px;
      z-index: 999;
    }
    .dropdown a {
      padding: 12px 20px;
      text-decoration: none;
      color: #f5f5f5;
      border-bottom: 1px solid #d4af37;
      transition: background 0.3s;
    }
    .dropdown a:hover { background-color: #3a3a3a; }

    /* Search Bar */
    .search-bar input {
      padding: 8px 15px;
      width: 300px;
      border-radius: 20px;
      border: none;
      outline: none;
      font-size: 16px;
    }

    /* Deals & Offerings */
    .deals {
      background-color: #d4af37;
      color: #0b0b0b;
      padding: 10px 20px;
      border-radius: 20px;
      font-weight: bold;
      cursor: pointer;
      transition: 0.3s;
    }
    .deals:hover { background-color: #b3952f; }

    /* Main Section */
    .main {
      padding: 60px 40px;
    }
    .section-title {
      font-family: 'Playfair Display', serif;
      font-size: 32px;
      margin-bottom: 20px;
      color: #d4af37;
    }

    /* Product Cards */
    .products {
      display: flex;
      gap: 30px;
      flex-wrap: wrap;
    }
    .product-card {
      background-color: #1a1a1a;
      border: 1px solid #d4af37;
      border-radius: 10px;
      padding: 20px;
      width: 220px;
      text-align: center;
      transition: transform 0.3s, box-shadow 0.3s;
    }
    .product-card:hover {
      transform: translateY(-10px);
      box-shadow: 0 0 20px #d4af37;
    }
    .product-card img {
      width: 100%;
      border-radius: 10px;
      margin-bottom: 15px;
    }
    .product-card h3 {
      margin-bottom: 10px;
      color: #f5f5f5;
    }
    .product-card p {
      color: #c7c7c7;
      font-size: 14px;
    }

    /* Footer */
    footer {
      background-color: #1a1a1a;
      padding: 40px;
      text-align: center;
      border-top: 1px solid #d4af37;
      margin-top: 60px;
    }
    footer a {
      color: #d4af37;
      margin: 0 10px;
      text-decoration: none;
      font-weight: bold;
    }
  </style>
</head>
<body>

  <!-- Top Nav -->
  <div class="topnav">
    <div class="hamburger" onclick="toggleDropdown()">
      <div></div>
      <div></div>
      <div></div>
    </div>
    <div class="search-bar">
      <input type="text" placeholder="Search EL DUCE">
    </div>
    <div class="deals">Deals & Offerings</div>
  </div>

  <!-- Dropdown -->
  <div class="dropdown" id="dropdownMenu">
    <a href="#">EL DUCE ITALIANO</a>
    <a href="#">EL DUCE JAPAN</a>
    <a href="#">EL DUCE BRAZIL</a>
  </div>

  <!-- Main Section -->
  <div class="main">
    <h2 class="section-title">Our Most Purchased Items</h2>
    <div class="products">
      <div class="product-card">
        <img src="https://via.placeholder.com/220x220" alt="Product 1">
        <h3>Product 1</h3>
        <p>$99</p>
      </div>
      <div class="product-card">
        <img src="https://via.placeholder.com/220x220" alt="Product 2">
        <h3>Product 2</h3>
        <p>$129</p>
      </div>
      <div class="product-card">
        <img src="https://via.placeholder.com/220x220" alt="Product 3">
        <h3>Product 3</h3>
        <p>$79</p>
      </div>
    </div>
  </div>

  <!-- Footer -->
  <footer>
    <p>Follow us on:</p>
    <a href="#">Instagram</a> | <a href="#">Facebook</a> | <a href="#">Twitter</a>
    <p>© 2025 EL DUCE. All Rights Reserved.</p>
  </footer>

  <script>
    function toggleDropdown() {
      const menu = document.getElementById('dropdownMenu');
      menu.style.display = menu.style.display === 'flex' ? 'none' : 'flex';
    }
  </script>

</body>
</html>
