<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>EL DUCE</title>
  <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;700&family=Roboto&display=swap" rel="stylesheet">
  <style>
    /* --- Reset & Fonts --- */
    * { margin:0; padding:0; box-sizing:border-box; font-family: 'Roboto', sans-serif; }
    body { background-color: #0b0b0b; color: #f5f5f5; overflow-x: hidden; }
    a { text-decoration: none; }

    /* --- Top Nav --- */
    .topnav {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 20px 40px;
      background-color: #1a1a1a;
      position: sticky;
      top: 0;
      z-index: 1000;
      transition: background 0.3s;
    }
    .topnav.scrolled { background-color: #111; }

    /* Hamburger menu */
    .hamburger { cursor: pointer; display: flex; flex-direction: column; gap: 5px; }
    .hamburger div { width: 25px; height: 3px; background-color: #d4af37; transition: 0.3s; }

    /* Dropdown menu */
    .dropdown {
      position: absolute;
      top: 60px;
      left: 40px;
      background-color: #1a1a1a;
      border: 1px solid #d4af37;
      display: none;
      flex-direction: column;
      min-width: 180px;
      z-index: 999;
    }
    .dropdown a { padding: 12px 20px; color: #f5f5f5; border-bottom: 1px solid #d4af37; transition: background 0.3s; }
    .dropdown a:hover { background-color: #3a3a3a; }

    /* Search Bar */
    .search-bar input {
      padding: 8px 15px;
      width: 300px;
      border-radius: 20px;
      border: none;
      outline: none;
      font-size: 16px;
    }

    /* Deals & Offerings */
    .deals {
      background-color: #d4af37;
      color: #0b0b0b;
      padding: 10px 20px;
      border-radius: 20px;
      font-weight: bold;
      cursor: pointer;
      transition: 0.3s;
    }
    .deals:hover { background-color: #b3952f; }

    /* --- Hero Banner --- */
    .hero {
      width: 100%;
      height: 70vh;
      background: url('https://images.unsplash.com/photo-1599058917212-27a7a5e98ee4?crop=entropy&cs=tinysrgb&fit=max&fm=jpg&ixid=Mnw5MTMyOXwwfDF8c2VhcmNofDF8fHRhbGwlMjBzaG9wfGVufDB8fHx8MTY5NzQyNjkyMw&ixlib=rb-4.0.3&q=80&w=1600') no-repeat center center/cover;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      text-align: center;
      color: #d4af37;
      position: relative;
      animation: fadeIn 1s ease-in;
    }
    .hero h1 { font-family: 'Playfair Display', serif; font-size: 60px; margin-bottom: 20px; text-shadow: 2px 2px 8px #000; }
    .hero p { font-size: 20px; margin-bottom: 30px; text-shadow: 1px 1px 5px #000; }
    .hero button {
      padding: 12px 30px;
      font-size: 18px;
      border: none;
      border-radius: 30px;
      background-color: #d4af37;
      color: #0b0b0b;
      cursor: pointer;
      transition: 0.3s;
    }
    .hero button:hover { background-color: #b3952f; }

    /* --- Main Section --- */
    .main { padding: 60px 40px; }

    /* Section titles */
    .section-title { font-family: 'Playfair Display', serif; font-size: 32px; margin-bottom: 20px; color: #d4af37; text-align: center; animation: fadeIn 1s ease-in; }

    /* Product Cards */
    .products, .collections { display: flex; gap: 30px; flex-wrap: wrap; justify-content: center; }
    .product-card, .collection-card {
      background-color: #1a1a1a;
      border: 1px solid #d4af37;
      border-radius: 10px;
      padding: 20px;
      width: 220px;
      text-align: center;
      transition: transform 0.3s, box-shadow 0.3s;
      opacity: 0;
    }
    .product-card img, .collection-card img { width: 100%; border-radius: 10px; margin-bottom: 15px; }
    .product-card h3, .collection-card h3 { margin-bottom: 10px; color: #f5f5f5; }
    .product-card p, .collection-card p { color: #c7c7c7; font-size: 14px; }
    .product-card:hover, .collection-card:hover { transform: translateY(-10px); box-shadow: 0 0 20px #d4af37; }

    /* Newsletter Signup */
    .newsletter {
      background-color: #1a1a1a;
      padding: 60px 40px;
      text-align: center;
      border: 1px solid #d4af37;
      border-radius: 10px;
      margin-top: 60px;
      animation: fadeIn 1s ease-in;
    }
    .newsletter input {
      padding: 12px 20px;
      width: 300px;
      border-radius: 30px;
      border: none;
      margin-right: 10px;
      font-size: 16px;
      outline: none;
    }
    .newsletter button {
      padding: 12px 25px;
      font-size: 16px;
      border: none;
      border-radius: 30px;
      background-color: #d4af37;
      color: #0b0b0b;
      cursor: pointer;
      transition: 0.3s;
    }
    .newsletter button:hover { background-color: #b3952f; }

    /* Footer */
    footer {
      background-color: #1a1a1a;
      padding: 40px;
      text-align: center;
      border-top: 1px solid #d4af37;
      margin-top: 60px;
    }
    footer a { color: #d4af37; margin: 0 10px; text-decoration: none; font-weight: bold; }

    /* --- Animations --- */
    @keyframes fadeIn { from { opacity: 0; } to { opacity: 1; } }
    .fadeIn { opacity: 1 !important; transition: opacity 1s ease-in; }
  </style>
</head>
<body>

  <!-- Top Nav -->
  <div class="topnav" id="topnav">
    <div class="hamburger" onclick="toggleDropdown()">
      <div></div>
      <div></div>
      <div></div>
    </div>
    <div class="search-bar">
      <input type="text" placeholder="Search EL DUCE">
    </div>
    <div class="deals">Deals & Offerings</div>
  </div>

  <!-- Dropdown -->
  <div class="dropdown" id="dropdownMenu">
    <a href="#">EL DUCE ITALIANO</a>
    <a href="#">EL DUCE JAPAN</a>
    <a href="#">EL DUCE BRAZIL</a>
  </div>

  <!-- Hero Banner -->
  <div class="hero">
    <h1>EL DUCE</h1>
    <p>Luxury streetwear with Italian flair</p>
    <button>Shop Now</button>
  </div>

  <!-- Our Most Purchased Items -->
  <div class="main">
    <h2 class="section-title">Our Most Purchased Items</h2>
    <div class="products">
      <div class="product-card"><img src="https://via.placeholder.com/220x220" alt="Product 1"><h3>Product 1</h3><p>$99</p></div>
      <div class="product-card"><img src="https://via.placeholder.com/220x220" alt="Product 2"><h3>Product 2</h3><p>$129</p></div>
      <div class="product-card"><img src="https://via.placeholder.com/220x220" alt="Product 3"><h3>Product 3</h3><p>$79</p></div>
    </div>
  </div>

  <!-- Featured Collections -->
  <div class="main">
    <h2 class="section-title">Featured Collections</h2>
    <div class="collections">
      <div class="collection-card"><img src="https://via.placeholder.com/220x220" alt="Collection 1"><h3>Italiano</h3><p>Premium Italian streetwear</p></div>
      <div class="collection-card"><img src="https://via.placeholder.com/220x220" alt="Collection 2"><h3>Japan</h3><p>Tokyo-inspired style</p></div>
      <div class="collection-card"><img src="https://via.placeholder.com/220x220" alt="Collection 3"><h3>Brazil</h3><p>Bold tropical designs</p></div>
    </div>
  </div>

  <!-- Newsletter Signup -->
  <div class="newsletter">
    <h2 class="section-title">Subscribe to Our Newsletter</h2>
    <p>Get exclusive deals and latest releases directly in your inbox</p>
    <input type="email" placeholder="Your Email">
    <button>Subscribe</button>
  </div>

  <!-- Footer -->
  <footer>
    <p>Follow us on:</p>
    <a href="#">Instagram</a> | <a href="#">Facebook</a> | <a href="#">Twitter</a>
    <p>© 2025 EL DUCE. All Rights Reserved.</p>
  </footer>

  <script>
    function toggleDropdown() {
      const menu = document.getElementById('dropdownMenu');
      menu.style.display = menu.style.display === 'flex' ? 'none' : 'flex';
    }

    // Animate elements on scroll
    const faders = document.querySelectorAll('.product-card, .collection-card, .newsletter, .section-title');
    const appearOptions = { threshold: 0.2, rootMargin: "0px 0px -50px 0px" };

    const appearOnScroll = new IntersectionObserver(function(entries, appearOnScroll) {
      entries.forEach(entry => {
        if (!entry.isIntersecting) return;
        entry.target.classList.add('fadeIn');
        appearOnScroll.unobserve(entry.target);
      });
    }, appearOptions);

    faders.forEach(fader => { appearOnScroll.observe(fader); });

    // Sticky topnav background change
    window.addEventListener('scroll', () => {
      const topnav = document.getElementById('topnav');
      topnav.classList.toggle('scrolled', window.scrollY > 50);
    });
  </script>

</body>
</html>
